# run: conda env create -f environment.yml
name: Adnerf
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python
  - numpy
  - pip
  - pip:
      - imageio
      - imageio-ffmpeg
      - ipdb
      - lpips
      - opencv-python>=4.4.0
      - Pillow>=7.2.0
      - pyyaml>=5.3.1
      - tensorboard>=2.4.0
      - imageio
      - imageio-ffmpeg
      - pymcubes
      - moviepy
      - matplotlib
      - scipy>=1.6.0
      - timm
      - einops
      - pynvml
      - tensorboard
      - scikit-image
      - opencv-python
      - svox
      - configargparse
  - pytorch
  - torchvision
  - cudatoolkit
  - pytorch-cuda=11.7
  - tqdm

